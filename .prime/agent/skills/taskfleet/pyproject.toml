[project]
name = "prime-agent-taskfleet"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["mcp>=1.10,<2"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/taskfleet"]
